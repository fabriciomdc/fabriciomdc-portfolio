export const Footer = () => {
  return (
    <footer className="border-t-2 border-foreground bg-foreground py-4 text-center">
      <span className="font-mono text-[9px] uppercase tracking-[0.15em] text-background">
        FAB.DEV — Portfolio / Built with intention / 2026
      </span>
    </footer>
  )
}
